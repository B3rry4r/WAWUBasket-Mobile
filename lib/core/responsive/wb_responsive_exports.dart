/// Barrel for the desktop-responsive layer. Import this one file to get the
/// breakpoints, the `context.isDesktop` helpers, the adaptive selectors, the
/// max-width wrapper, and the desktop nav chrome (side-nav, dashboard shell,
/// web top bar).
library;

export 'wb_adaptive.dart';
export 'wb_breakpoints.dart';
export 'wb_dashboard_shell.dart';
export 'wb_max_width.dart';
export 'wb_responsive.dart';
export 'wb_side_nav.dart';
export 'wb_web_top_bar.dart';
