source 'https://rubygems.org'

ruby '3.1.7'

# Pin Fastlane and its dependencies to avoid resolver conflicts in CI.
gem 'fastlane', '~> 2.224'
# CocoaPods needed for Flutter iOS build in CI
gem 'cocoapods', '~> 1.16'
# cocoapods-core requires public_suffix ~> 4.0
gem 'public_suffix', '~> 4.0'
